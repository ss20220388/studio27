---
interface Kurs {
    id: number;
    naziv: string;
    opis: string;
    cena: number;
    trajanje: number;
    slikaUrl: string;
}

const { mojiKursevi } = Astro.props as { mojiKursevi: Kurs[] };
import Java from "../assets/sta-je-java.jpg";
import Spring from "../assets/images.png";
import React from "../assets/react.png";
import type { ImageMetadata } from "astro";

let slika: ImageMetadata;

if (mojiKursevi[0].naziv.includes("Java")) {
    slika = Java;
} else if (mojiKursevi[0].naziv.includes("Spring")) {
    slika = Spring;
} else if (mojiKursevi[0].naziv.includes("React")) {
    slika = React;
} else {
    slika = Java; // fallback
}
---

<section
    class="shadow-md bg-white rounded-xl p-6 hover:shadow-xl transition duration-300 cursor-pointer w-full"
>
    <img
        src={slika.src}
        alt={mojiKursevi[0].naziv}
        class="w-full h-40 object-cover rounded-lg mb-4"
    />

    <h2 class="text-xl font-bold text-gray-800 mb-2">
        {mojiKursevi[0].naziv}
    </h2>

    <p class="text-gray-600 mb-3">
        {mojiKursevi[0].opis}
    </p>

    <p class="font-semibold text-indigo-600">
        {mojiKursevi[0].cena} RSD
    </p>

    <p class="text-sm text-gray-500">
        {mojiKursevi[0].trajanje} časova
    </p>
</section>
