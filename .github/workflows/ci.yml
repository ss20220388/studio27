name: CI/CD Pipeline

on:
  pull_request:
    branches: [ main, staging, production ]
  push:
    branches: [ main, staging, production ]

jobs:
  quick-check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - run: echo "✅ Code committed successfully"
