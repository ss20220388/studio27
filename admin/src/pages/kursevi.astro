---
import Layout from "../layouts/Layout.astro";
import CoursesPage from "../react components/CoursesPage";
const accesToken = Astro.cookies.get("refreshToken")?.value || null;
---

<Layout title="Kursevi">
    <CoursesPage accesToken={accesToken} client:load />
</Layout>